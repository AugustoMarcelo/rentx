import React from 'react';
import { View, StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    height: 2,
    backgroundColor: '#DC1637',
  },
});

export default function SliderRailSelected() {
  return <View style={styles.container} />;
}
